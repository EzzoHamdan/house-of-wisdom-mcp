# Sample Repo

A tiny fixture for testing the agentic synthesizer's tool loop.

## Key facts (for the synthesizer to verify)

- `src/math.py` defines `add(a, b)` and `divide(a, b)`.
- `divide` raises `ValueError` when `b == 0`.
- There is no `subtract` function in this repo.